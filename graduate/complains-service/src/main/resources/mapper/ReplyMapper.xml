<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cuit.mapper.ReplyMapper">
    <insert id="insertNew">
        insert into t_reply (pid,uid,re_name,re_content,re_time,to_uid)
        values (#{pid},#{uid},#{reName},#{reContent},now(),#{toUid})
    </insert>
    <select id="selectAllReply" resultType="cn.cuit.entity.Reply">

    </select>
</mapper>
